package edu.neu.mgen;

public class HW5_3 {
    public static void main(String[] args) {
        int x = 10;
        int y = 5; 
        System.out.println("the value of x =" + x + "\nthe value of y =" + y);
        /*
         * The Math class in Java is part of the java.lang package and provides a collection of 
         * static methods and constants that help perform various mathematical operations. 

            Description: Returns the greater of two values.
            Parameters: This method can accept two arguments of types int, long, float, or double.
            Return Type: The same type as the passed arguments.
            Example:
            java
            Copy code
            int result = Math.max(5, 8);  // result is 8
            min(a, b):

            Description: Returns the smaller of two values.
            Parameters: This method can accept two arguments of types int, long, float, or double.
            Return Type: The same type as the passed arguments.
            Example:
            java
            Copy code
            int result = Math.min(5, 8);  // result is 5
            sqrt(x):

            Description: Returns the square root of a value.
            Parameters: Accepts a single argument of type double.
            Return Type: double.
            Note: If the argument is negative, the result is NaN (Not a Number). If the argument is positive infinity, the result is positive infinity.
            Example:
            java
            Copy code
            double result = Math.sqrt(25);  // result is 5.0
         */
    }
    
}
